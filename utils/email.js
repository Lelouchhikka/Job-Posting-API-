const sendEmail = (recruiterEmail, jobTitle) => {
    console.log(`Email to ${recruiterEmail}: New application for job ID ${jobTitle}`);
  };
  
  module.exports = sendEmail;
//Simulates sending an email notification (via console log) when an application is submitted.  